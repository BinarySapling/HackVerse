# HackVerse Frontend — copy to .env (never commit .env)

# Backend API base URL (must include /api/v1)
# Local:
# VITE_API_URL=http://localhost:5000/api/v1

# Production:
VITE_API_URL=https://your-api.example.com/api/v1
